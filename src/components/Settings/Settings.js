import setClass from './Settings.module.css';
import React from "react";

const Settings = () => {
    return (
        <div className={setClass.main}>
            Settings Settings Settings
        </div>
    )
}

export default Settings;